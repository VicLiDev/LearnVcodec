SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)

AUX_SOURCE_DIRECTORY(. SYSTEMTIME_SOURCE)

ADD_EXECUTABLE(systemtime ${SYSTEMTIME_SOURCE})

TARGET_LINK_LIBRARIES(systemtime comm onvif ${LIBS})
