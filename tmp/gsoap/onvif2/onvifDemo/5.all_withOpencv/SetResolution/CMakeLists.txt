SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)

AUX_SOURCE_DIRECTORY(. SETRESOLUTION_SOURCE)

ADD_EXECUTABLE(SetResolution ${SETRESOLUTION_SOURCE})

TARGET_LINK_LIBRARIES(SetResolution comm onvif ${LIBS})
