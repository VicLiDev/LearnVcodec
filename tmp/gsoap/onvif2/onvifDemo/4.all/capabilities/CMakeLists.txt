SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)

AUX_SOURCE_DIRECTORY(. CAPABILITIES_SOURCE)

ADD_EXECUTABLE(capabilities ${CAPABILITIES_SOURCE})

TARGET_LINK_LIBRARIES(capabilities comm onvif ${LIBS})
