SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)

AUX_SOURCE_DIRECTORY(. DEVICEINFO_SOURCE)

ADD_EXECUTABLE(deviceinfo ${DEVICEINFO_SOURCE})

TARGET_LINK_LIBRARIES(deviceinfo comm onvif ${LIBS})
